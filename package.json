{
  "name": "sheepskin-js",
  "version": "0.0.2",
  "description": "Calendar and schedule library for javascript",
  "main": "lib/Sheepskin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src --out-dir ./lib --source-maps --presets stage-0",
    "deploy": "yarn run build && git add . && yarn version && git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:LoneBeta/sheepskin-js.git"
  },
  "author": "Andrew Turnbull",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "prettier": "^1.7.0"
  },
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2018": "^1.0.0",
    "babel-preset-stage-0": "^6.24.1"
  },
  "peerDependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.1"
  },
  "private": true
}
